<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.wyu.infolib.dao.UserDeviceMapper" >
  <resultMap id="BaseResultMap" type="com.wyu.infolib.entity.UserDevice" >
    <result column="device_id" property="deviceId" jdbcType="INTEGER" />
    <result column="user_id" property="userId" jdbcType="INTEGER" />
  </resultMap>
  <insert id="insert" parameterType="com.wyu.infolib.entity.UserDevice" >
    insert into user_device (device_id, user_id)
    values (#{deviceId,jdbcType=INTEGER}, #{userId,jdbcType=INTEGER})
  </insert>
  <insert id="insertSelective" parameterType="com.wyu.infolib.entity.UserDevice" >
    insert into user_device
    <trim prefix="(" suffix=")" suffixOverrides="," >
      <if test="deviceId != null" >
        device_id,
      </if>
      <if test="userId != null" >
        user_id,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides="," >
      <if test="deviceId != null" >
        #{deviceId,jdbcType=INTEGER},
      </if>
      <if test="userId != null" >
        #{userId,jdbcType=INTEGER},
      </if>
    </trim>
  </insert>
  <select id="find" resultMap="BaseResultMap">
    select
    device_id, user_id
    from user_device
    where device_id = #{deviceId,jdbcType=INTEGER}
    and user_id = #{userId,jdbcType=INTEGER}
  </select>
  <select id="findByUserId" resultMap="BaseResultMap">
    select
    device_id, user_id
    from user_device
    where user_id = #{userId,jdbcType=INTEGER}
  </select>
  <delete id="delete" parameterType="com.wyu.infolib.entity.UserDevice">
    delete from user_device
    where user_id = #{userId,jdbcType=INTEGER}
    and device_id = #{deviceId,jdbcType=INTEGER}
  </delete>
</mapper>